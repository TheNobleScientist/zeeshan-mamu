rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /shows/{year}/{slugFolder}/{filename} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.email == "muhammadzainb@gmail.com";
    }
  }
}
